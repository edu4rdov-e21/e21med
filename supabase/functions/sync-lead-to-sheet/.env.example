# Apenas NOMES das variáveis — nunca valores reais.
# Em produção, defina via `supabase secrets set` (ver README.md).
# Para teste local, copie este arquivo pra `.env` (gitignorado) e preencha.

WEBHOOK_SECRET=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
GOOGLE_SHEET_ID=
GOOGLE_SHEET_TAB=
